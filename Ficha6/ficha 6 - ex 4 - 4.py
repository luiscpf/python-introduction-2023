def cria_lista_nmultiplos (numero, multiplicador):
    if isinstance(numero, multiplicador, int) and numero > 0:
        lista_retorno = []
        while countador < numero2:
            lista_retorno += [elemento*numero]
        return lista_retorno
    else:
        raise ValueError ("O número indicado não é inteiro positivo")